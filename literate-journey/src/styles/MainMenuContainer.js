import styled from 'styled-components';

export const MainMenuContainer = styled.nav`
    ul {
        list-style: none;
        padding: 0;
        li (
            display: inline-black;
            margin-right: 10 px;
        )
    }
`